<%
  if @category
    seo_fields_renderer = DiscourseSeo::CategorySeoFieldsRenderer.new(@category, params)
    @title = seo_fields_renderer.field_or_template(:meta_title)
    @description_meta = seo_fields_renderer.field_or_template(:meta_description)
    @keywords_meta = seo_fields_renderer.field_or_template(:meta_keywords)
  end
%>
